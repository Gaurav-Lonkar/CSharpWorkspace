
@{
    ViewBag.Title = "topkunal";

}
<h2>top kunal names</h2>
@{
    ViewBag.Title = "pahile";
    var grid = new WebGrid(source: Model);

}

@grid.GetHtml(columns: grid.Columns(


    grid.Column(columnName: "fname", header: "First Name"),
    grid.Column(columnName: "lname", header: "Last Name"),
    grid.Column(columnName: "email", header: "Email Address"),
    grid.Column(columnName: "phone", header: "Mobile Number"),
    grid.Column(columnName: "City", header: "Location")
    )
    )



